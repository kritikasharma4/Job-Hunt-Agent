# AI Job Hunting Agent - Environment Configuration Template

# ===== LLM Configuration =====
# Provider: ollama, openai, anthropic, google, etc.
LLM_PROVIDER=ollama

# Model name (for ollama: llama3, llama2, mistral, etc.)
LLM_MODEL=llama3

# API key (not needed for ollama)
# LLM_API_KEY=your-api-key-here

# Model parameters
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2048
LLM_TIMEOUT=30

# ===== Job Fetcher Configuration =====
# Enabled job sources (comma-separated or space-separated in CLI)
JOB_SOURCES=linkedin,indeed

# Maximum results per source
MAX_RESULTS_PER_SOURCE=50

# Rate limiting between requests (seconds)
RATE_LIMIT_DELAY=1.0

# ===== Relevance Matching Configuration =====
# Minimum relevance score threshold (0.0 to 1.0)
MIN_RELEVANCE_SCORE=0.6

# Component weights (should sum to 1.0)
WEIGHT_SKILLS=0.3
WEIGHT_EXPERIENCE=0.3
WEIGHT_LOCATION=0.2
WEIGHT_SALARY=0.2

# ===== Job Filtering Configuration =====
# Salary range filters (optional)
# MIN_SALARY=50000
# MAX_SALARY=150000

# Preferred job locations (comma-separated)
# PREFERRED_LOCATIONS=San Francisco,New York,Boston

# Experience level requirements
MIN_EXPERIENCE_YEARS=0

# ===== Application Configuration =====
DEBUG=false
LOG_LEVEL=INFO

# Output format: json, csv, html
OUTPUT_FORMAT=json

# ===== Database Configuration (optional) =====
# DATABASE_URL=sqlite:///job_hunt.db
# Or use PostgreSQL: postgresql://user:password@localhost/job_hunt
